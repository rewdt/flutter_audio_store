import 'package:flutter/material.dart';

class AppWelcomePage extends StatelessWidget {
  const AppWelcomePage();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child:Text('Welcome to the welcome page')
      )
    );
  }
}